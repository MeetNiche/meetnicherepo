# ===============================
# = DataSource Configuration =
# ===============================
#spring.datasource.url=jdbc:mysql://localhost:3306/meetniche?useSSL=false&serverTimezone=UTC
#spring.datasource.username=meetniche
#spring.datasource.password=meetniche
#spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Hibernate JPA Configuration
spring.jpa.hibernate.ddl-auto=update   # Use `create` only for development, then `update` or `validate` for production.
spring.jpa.show-sql=true               # Shows SQL statements in the console.
spring.jpa.properties.hibernate.format_sql=true

# ===============================
# = Spring Boot Server Config =
# ===============================
# Specify the port your application runs on (default: 8080)
server.port=8080

# ===============================
# = JWT Configuration =
# ===============================
jwt.secret=your_jwt_secret_key   # Replace with a strong, random secret key
jwt.expiration=86400000          # JWT expiration time in milliseconds (e.g., 1 day)

# ===============================
# = Mail Configuration =
# ===============================
# Set up mail configurations for notifications
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=roshp9863@gmail.com          # Replace with your email
spring.mail.password=Rosh@n!123           # Replace with your email password
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.ssl.protocols=TLSv1.2

# ===============================
# = Thymeleaf Configuration (Optional) =
# ===============================
spring.thymeleaf.cache=false       # Disable caching during development


# Logging Configuration
logging.level.root=INFO
logging.level.com.product.meetniche=DEBUG  # Adjust based on your package structure

